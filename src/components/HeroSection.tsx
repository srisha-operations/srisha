const HeroSection = () => {
  return (
    <section className="w-full h-[400px] flex items-center justify-center border">
      Hero Section Placeholder
    </section>
  );
};

export default HeroSection;
